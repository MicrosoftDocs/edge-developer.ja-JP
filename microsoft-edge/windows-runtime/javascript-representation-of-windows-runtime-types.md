---
title: Windows ランタイム型の JavaScript 表現
ms.custom: ''
ms.date: 07/29/2020
ms.prod: microsoft-edge
ms.reviewer: ''
ms.suite: ''
ms.technology: windows-integration
ms.tgt_pltfrm: ''
ms.topic: article
helpviewer_keywords:
- Windows Runtime types [JavaScript]
- JavaScript, Windows Runtime types
ms.assetid: f4851802-8b40-4afa-ba6c-8a162a9a43cc
caps.latest.revision: 9
author: MSEdgeTeam
ms.author: msedgedevrel
manager: ''
ms.openlocfilehash: 0ee55b5dd5071b0f0b31656ae164e9801e98d6ac
ms.sourcegitcommit: 29cbe0f464ba0092e025f502833eb9cc3e02ee89
ms.translationtype: MT
ms.contentlocale: ja-JP
ms.lasthandoff: 08/20/2020
ms.locfileid: "10942044"
---
# <span data-ttu-id="42fac-102">Windows ランタイムの種類の JavaScript 表現</span><span class="sxs-lookup"><span data-stu-id="42fac-102">JavaScript representation of Windows Runtime types</span></span>  

[!INCLUDE [deprecation-note](../includes/legacy-edge-note.md)]  

<span data-ttu-id="42fac-103">次の表では、JavaScript が Windows ランタイムの種類を表す方法について説明します。</span><span class="sxs-lookup"><span data-stu-id="42fac-103">The following table describes the way JavaScript represents Windows Runtime types.</span></span>  

> [!IMPORTANT]
> <span data-ttu-id="42fac-104">Internet Explorer で実行されているアプリでは、Windows ランタイム機能は使用できません。</span><span class="sxs-lookup"><span data-stu-id="42fac-104">Windows Runtime features are not available for apps that run in Internet Explorer.</span></span>  

## <span data-ttu-id="42fac-105">JavaScript の Windows ランタイムの種類</span><span class="sxs-lookup"><span data-stu-id="42fac-105">Windows Runtime types in JavaScript</span></span>  

| <span data-ttu-id="42fac-106">Windows ランタイムの種類</span><span class="sxs-lookup"><span data-stu-id="42fac-106">Windows Runtime type</span></span> | <span data-ttu-id="42fac-107">JavaScript 表現</span><span class="sxs-lookup"><span data-stu-id="42fac-107">JavaScript representation</span></span> | <span data-ttu-id="42fac-108">説明</span><span class="sxs-lookup"><span data-stu-id="42fac-108">Description</span></span> |  
|:--- |:--- |:--- |  
| `UInt8` | `Number` | <span data-ttu-id="42fac-109">Windows ランタイム `Uint8` は、JavaScript 番号として表される符号なし 8 ビット整数です。</span><span class="sxs-lookup"><span data-stu-id="42fac-109">A Windows Runtime `Uint8` is an unsigned 8-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="42fac-110">最初に JavaScript 値が JavaScript 番号に変換され、その後、 `ToUint32` プロセスの次に進みます。</span><span class="sxs-lookup"><span data-stu-id="42fac-110">A JavaScript value is first converted to a JavaScript number, and then the `ToUint32` process is followed.</span></span>  <span data-ttu-id="42fac-111">JavaScript 番号の値が Uint8 の範囲外の場合、結果はモジュール 2^8 が適用されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-111">If the JavaScript number value is outside the range of a Uint8, the result will have modulo 2^8 applied.</span></span>  |  
| `Int32` | `Number` | <span data-ttu-id="42fac-112">Windows ランタイムは、JavaScript 番号として表される符号付き `Int32` 32 ビット整数です。</span><span class="sxs-lookup"><span data-stu-id="42fac-112">A Windows Runtime `Int32` is a signed 32-bit integer, represented as a JavaScript number.</span></span>  <span data-ttu-id="42fac-113">最初に JavaScript 値が JavaScript 番号に変換され、その後、 `ToInt32` プロセスの次に進みます。</span><span class="sxs-lookup"><span data-stu-id="42fac-113">A JavaScript value is first converted to a JavaScript number, and then the `ToInt32` process is followed.</span></span>  <span data-ttu-id="42fac-114">JavaScript 番号の値が範囲外の場合、 `Int32` 結果はモジュール 2^16 が適用されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-114">If the JavaScript number value is outside the range of an `Int32`, the result will have modulo 2^16 applied.</span></span>  |  
| `Int64` | `Number` | <span data-ttu-id="42fac-115">Windows ランタイム `Int64` は、標準の 64 ビット整数で、標準の数値が \[-2^53, 2^53\] の範囲内に含まれています。</span><span class="sxs-lookup"><span data-stu-id="42fac-115">A Windows Runtime `Int64` is a signed 64-bit integer, represented as a standard number if it falls within the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="42fac-116">この範囲外にある場合、その値は、64 ビットの整数データを格納するカスタム バックアップ ストアを持つ数値として表されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-116">If it falls outside this range, it is represented as a number value that has a custom backing store that maintains the full 64 bits of integer data.</span></span>  <span data-ttu-id="42fac-117">これらのカスタム Int64 数値の値に対するすべての算処理により、値は \[-2^53, 2^53\] の範囲内の標準の数値に変換されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-117">All mathematical operations on these custom Int64 number values cause the value to be converted to a standard number in the range \[-2^53, 2^53\].</span></span>  <span data-ttu-id="42fac-118">値がこの範囲外の場合、型エラーが発生します。</span><span class="sxs-lookup"><span data-stu-id="42fac-118">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="42fac-119">この変換処理の例外は `==` `===` `SameValue` `RelationalComparison` 、2 つのビット値を表す 64 ビットを超える引数を比較する、および操作です。</span><span class="sxs-lookup"><span data-stu-id="42fac-119">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two represented 64-bit values.</span></span>  <span data-ttu-id="42fac-120">いずれかの引数が標準 JavaScript 番号の場合、比較する前に、これらの引数を JavaScript 番号に変換することもできます。</span><span class="sxs-lookup"><span data-stu-id="42fac-120">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="42fac-121">JavaScript 値が Int64 値自体の場合は直接割り当てられます。それ以外の場合は、値に対する `ToInteger` 変換を適用した結果が Windows ランタイムに転送されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-121">A JavaScript value is directly assigned if it is an Int64 value itself; otherwise the result of applying the `ToInteger` conversion on the value is passed to Windows Runtime.</span></span>  <span data-ttu-id="42fac-122">型の補正に失敗した場合は、例外が返されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-122">If the type coercion fails, an exception is returned.</span></span>  |  
| `Uint64` | `Number` | <span data-ttu-id="42fac-123">Windows ランタイム `Uint64` は、標準の数値として表されます (標準の数値が \[0, 2^53\]])。</span><span class="sxs-lookup"><span data-stu-id="42fac-123">A Windows Runtime `Uint64` is an unsigned 64-bit integer, represented as a standard number if it falls within the range \[0, 2^53\].</span></span>  <span data-ttu-id="42fac-124">この範囲外にある場合、それはサインインされていない整数データのうえ 64 ビットを保持するカスタム バックアップ ストアを持つ番号と表されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-124">If it falls outside this range, it is represented as a number that has a custom backing store that maintains the full 64 bits of unsigned integer data.</span></span>  <span data-ttu-id="42fac-125">これらのカスタム Uint64 値に対するすべての算処理により、値は \[0, 2^53\] の範囲内の標準の数値に変換されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-125">All mathematical operations on these custom Uint64 values cause the value to be converted to a standard number in the range \[0, 2^53\].</span></span>  <span data-ttu-id="42fac-126">値がこの範囲外の場合、型エラーが発生します。</span><span class="sxs-lookup"><span data-stu-id="42fac-126">If the value is outside this range, a type error is raised.</span></span>  <span data-ttu-id="42fac-127">この変換処理の例外は、2 つの 64 ビット値に基づいて引数を比較する `==` `===` `SameValue` `RelationalComparison` 、および操作です。</span><span class="sxs-lookup"><span data-stu-id="42fac-127">The exceptions to this conversion process are the `==`, `===`, `SameValue`, and `RelationalComparison` operations, which compare the arguments based on the full 64 bits when passed two 64-bit values.</span></span>  <span data-ttu-id="42fac-128">いずれかの引数が標準 JavaScript 番号の場合、比較する前に、これらの引数を JavaScript 番号に変換することもできます。</span><span class="sxs-lookup"><span data-stu-id="42fac-128">If either argument is a standard JavaScript number, these operations also convert the argument to a JavaScript number before comparison.</span></span>  <span data-ttu-id="42fac-129">Uint64 値自体の場合は、JavaScript 値が直接割り当てられます。それ以外の場合、値に対して変換を適用した後にモジュール `ToInteger` 2^52 を押した後に modulo 2^52 を押すと、Windows ランタイムに換えられます。</span><span class="sxs-lookup"><span data-stu-id="42fac-129">A JavaScript value is directly assigned if it is a Uint64 value itself; otherwise, the result of applying the `ToInteger` conversion on the value, followed by taking modulo 2^52, is passed to Windows Runtime.</span></span>  <span data-ttu-id="42fac-130">型の変換に失敗すると、例外が返されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-130">If the type conversion fails, an exception is returned.</span></span>  |  
| `Single` | `Number` | <span data-ttu-id="42fac-131">Windows ランタイムは、単精度値に最も多くの精度値を選択することで、JavaScript 番号として表される 32 ビットのフローティ `Single` ング ポイント番号です。</span><span class="sxs-lookup"><span data-stu-id="42fac-131">A Windows Runtime `Single` is a 32-bit floating-point number, represented as a JavaScript number by picking the closest double-precision value to the single-precision value.</span></span>  <span data-ttu-id="42fac-132">JavaScript 値は JavaScript 番号に変換され、範囲がオンになり、単精度の 32 ビット IEEE 754 フローティング ポイント番号で値を表すことができます。</span><span class="sxs-lookup"><span data-stu-id="42fac-132">A JavaScript value is converted to a JavaScript number and then range checked to ensure that the value can be represented in a single-precision 32-bit IEEE 754 floating-point number.</span></span>  <span data-ttu-id="42fac-133">変換または範囲のチェックが失敗した場合は、マーシュアリング エラーが返されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-133">If the conversion or range check fails, a marshaling error is returned.</span></span>  |  
| `Double` | `Number` | <span data-ttu-id="42fac-134">Windows ランタイム `Double` は 64 ビットのフローティング ポイント番号です。</span><span class="sxs-lookup"><span data-stu-id="42fac-134">A Windows Runtime `Double` is a 64-bit floating-point number.</span></span>  `ToNumber` <span data-ttu-id="42fac-135">は、Windows ランタイムを `Double` JavaScript 番号に変換するために使用されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-135">is used to convert a Windows Runtime `Double` to a JavaScript number.</span></span>  <span data-ttu-id="42fac-136">変換が失敗した場合は、マーシュアリング エラーが返されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-136">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Boolean` | `Boolean` | <span data-ttu-id="42fac-137">Windows ランタイム `Boolean` は 8 ビット値であり、0 以外 `false` の値は `true` JavaScript として表されます `Boolean` 。</span><span class="sxs-lookup"><span data-stu-id="42fac-137">A Windows Runtime `Boolean` is an 8-bit value where zero is `false` and any value other than zero is `true`, represented as a JavaScript `Boolean`.</span></span>  <span data-ttu-id="42fac-138">最初に JavaScript 値が JavaScript に変換 `Boolean` されます `ToBoolean` 。</span><span class="sxs-lookup"><span data-stu-id="42fac-138">A JavaScript value is first converted to a JavaScript `Boolean` by `ToBoolean`.</span></span>  <span data-ttu-id="42fac-139">つまり、JavaScript で記述されると認めた Windows ラン `void func(BOOL);` タイム関数が示されます `obj.func("test");` 。</span><span class="sxs-lookup"><span data-stu-id="42fac-139">This means that a Windows Runtime function declared as `void func(BOOL);` could be written in JavaScript as `obj.func("test");`.</span></span>  <span data-ttu-id="42fac-140">Windows ランタイム関数は、パラメータ `BOOL` ーとしてパラメーターされたパラメーターを使用して呼び出されます `TRUE` 。</span><span class="sxs-lookup"><span data-stu-id="42fac-140">The Windows Runtime function is called with the `BOOL` parameter passed as `TRUE`.</span></span>  <span data-ttu-id="42fac-141">変換が失敗した場合は、マーシュアリング エラーが返されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-141">If the conversion fails, a marshaling error is returned.</span></span>  |  
| `Char16` | `String` | <span data-ttu-id="42fac-142">Windows ランタイムは 16 ビット Unicode 文字で、1 文字を含む `Char16` JavaScript 文字列と表されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-142">A Windows Runtime `Char16` is a 16-bit Unicode character, represented as a JavaScript string that contains a single character.</span></span>  <span data-ttu-id="42fac-143">JavaScript 値は JavaScript 文字列に変換され、その文字列が 1 文字の長さのみである `ToString` ことを確認します。</span><span class="sxs-lookup"><span data-stu-id="42fac-143">A JavaScript value is converted to a JavaScript string by `ToString` and then checked to ensure that the string is only a single character long.</span></span>  <span data-ttu-id="42fac-144">その後、1 文字が値として入力 `Char16` されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-144">The single character is then passed as the `Char16` value.</span></span>  <span data-ttu-id="42fac-145">変換または長さがチェックに失敗した場合は、マーシュアリング エラーが返されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-145">If the conversion or length check fails, a marshaling error is returned.</span></span>  |  
| `String` | `String` | <span data-ttu-id="42fac-146">Windows ランタイムは、オブジェクトがよく使用される一覧 `String` `Char16` です。</span><span class="sxs-lookup"><span data-stu-id="42fac-146">A Windows Runtime `String` is an immutable sequence of `Char16` objects.</span></span>  <span data-ttu-id="42fac-147">文字列は JavaScript オブジェクトとして `String` 表されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-147">Strings are represented as JavaScript `String` objects.</span></span>  <span data-ttu-id="42fac-148">Windows ランタイム文字列には、"\("\) には異なる値が `null` 含られていません。</span><span class="sxs-lookup"><span data-stu-id="42fac-148">Windows Runtime strings do not have different values for `null` and empty string \(""\).</span></span>  `null` <span data-ttu-id="42fac-149">また、文字列値は JavaScript の文字列に変換されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-149">and empty string values are converted to the JavaScript empty string.</span></span>  <span data-ttu-id="42fac-150">**注**: JavaScript が文字列を予想する Windows ランタイム パラメーターにパスされると `null` 、文字列値 "null" を返します。または `null` 、""\) が返されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-150">**NOTE**:  When JavaScript `null` is passed to a Windows Runtime parameter that expects a string, it produces the string value "null", not `null` or the empty string \(""\).</span></span>  <span data-ttu-id="42fac-151">Windows ランタイムにパスされる文字列は、常に、常に、白一の文字列にインスタンス化する必要があります。</span><span class="sxs-lookup"><span data-stu-id="42fac-151">Strings passed to the Windows Runtime should always be instantiated to the empty string.</span></span>  |  
| `Enumeration` | `Object` | <span data-ttu-id="42fac-152">Windows ランタイムは 32 ビット整数 \(署名または署名されていない\) で、名前付き定数が関連付けられて `Enumeration` います。</span><span class="sxs-lookup"><span data-stu-id="42fac-152">A Windows Runtime `Enumeration` is a 32-bit integer \(signed or unsigned\) that has an associated set of named constants.</span></span>  <span data-ttu-id="42fac-153">JavaScript では、列挙型は、名前付きの各名前付き値の読み取り専用フィールドを含むオブジェクトとして表されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-153">In JavaScript, an enumeration is represented as an object that contains a read-only field for each named value.</span></span>  <span data-ttu-id="42fac-154">列挙値は、前に定義されたとおり、JavaScript 番号に変換 `Int32` されます `UInt32` 。</span><span class="sxs-lookup"><span data-stu-id="42fac-154">Enumeration values are converted to JavaScript numbers as defined earlier for `Int32` and `UInt32`.</span></span>  <span data-ttu-id="42fac-155">JavaScript 値が Windows ランタイム列挙型に変換されると、前述したとおり、変換、切り捨て、および範囲がオンになっています。</span><span class="sxs-lookup"><span data-stu-id="42fac-155">When a JavaScript value is converted to a Windows Runtime enumeration, it is converted, truncated and range checked as described earlier.</span></span>  <span data-ttu-id="42fac-156">ただし、生じる値は、列挙型で指定されている定義された名前付き値に対してチェックされません。</span><span class="sxs-lookup"><span data-stu-id="42fac-156">However, the resulting value is not checked against the defined named values that are specified in the enumeration.</span></span>  |  
| `Structure` | `Object` | <span data-ttu-id="42fac-157">Windows ランタイムは、名前付きデータ フィールドの大き `Structure` なコレクションです。</span><span class="sxs-lookup"><span data-stu-id="42fac-157">A Windows Runtime `Structure` is a heterogeneous collection of named data fields.</span></span>  <span data-ttu-id="42fac-158">JavaScript では、構造体内の各フィールドの名前付きプロパティを含む JavaScript オブジェクトとして構造体が表示されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-158">In JavaScript, a structure is represented as a JavaScript object that contains a named property for each field in the structure.</span></span>  <span data-ttu-id="42fac-159">構造値の変換に失敗した場合は、マーシュアリング エラーが返されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-159">If the conversion of any structure value fails, a marshaling error is returned.</span></span>  <span data-ttu-id="42fac-160">Windows ランタイム構造体に同等のものがない JavaScript オブジェクトのフィールドは無視されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-160">Fields in the JavaScript object that do not have equivalents in the Windows Runtime structure are ignored.</span></span>  <span data-ttu-id="42fac-161">**注**: JavaScript では、キーワードを使用して Windows ランタイム構造体をインスタンス化 `new` することはできません。</span><span class="sxs-lookup"><span data-stu-id="42fac-161">**NOTE**:  A Windows Runtime structure cannot be instantiated in JavaScript with the `new` keyword.</span></span>  |  
| `Array` | `Array` | <span data-ttu-id="42fac-162">Windows ランタイム `Array` は JavaScript 配列に変換されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-162">A Windows Runtime `Array` is converted to a JavaScript array.</span></span>  <span data-ttu-id="42fac-163">ただし、Windows ランタイム配列はサイズ変更できないため、一部の JavaScript 配列操作を実行することはできません。</span><span class="sxs-lookup"><span data-stu-id="42fac-163">However, Windows Runtime arrays are not resizable, so some JavaScript array operations are not possible.</span></span>  <span data-ttu-id="42fac-164">変換された配列の内部プロパティは `[[Class]]` ECMAScript 5 指定で許可されないため、"Array" に設定されていません。</span><span class="sxs-lookup"><span data-stu-id="42fac-164">The internal `[[Class]]` property of a converted array is not set to "Array" because this is not allowed by the ECMAScript 5 specification.</span></span>  <span data-ttu-id="42fac-165">つまり、返 `Array.isArray(v)` される値を指定します `false` 。</span><span class="sxs-lookup"><span data-stu-id="42fac-165">This means that `Array.isArray(v)` returns `false`.</span></span>  <span data-ttu-id="42fac-166">JavaScript 値は次のように Windows ランタイム配列に変換されます。値が含まれます。または、ネイティブに `null` `undefined` 変換されます `null` 。</span><span class="sxs-lookup"><span data-stu-id="42fac-166">A JavaScript value is converted to a Windows Runtime array as follows:  If the value is `null` or `undefined`, it is converted to a native `null`.</span></span>  <span data-ttu-id="42fac-167">内部プロパティが "Array" の場合、それがネイティブ配列にコピーされ、この配 `[[Class]]` 列への参照が合算されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-167">If its internal `[[Class]]` property is "Array", it is copied to a native array and a reference to this array is passed.</span></span>  <span data-ttu-id="42fac-168">前述のように変換された配列の場合、基になるネイティブ配列が実行されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-168">If it is a converted array, as described previously, the underlying native array is passed.</span></span>  <span data-ttu-id="42fac-169">**注**: JavaScript 配列値を Windows ランタイム メソッドに指定すると、配列の完全コピーが作成されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-169">**NOTE**:  Passing a JavaScript array value to a Windows Runtime method causes a full copy of the array.</span></span>  |  
| <span data-ttu-id="42fac-170">代理人 \(関数コールバック\)</span><span class="sxs-lookup"><span data-stu-id="42fac-170">Delegate \(function callback\)</span></span> | `Function` | <span data-ttu-id="42fac-171">Windows ランタイム代理人は、単一の方法への参照です。</span><span class="sxs-lookup"><span data-stu-id="42fac-171">A Windows Runtime delegate is a reference to a single method.</span></span>  <span data-ttu-id="42fac-172">Windows ランタイム代理人は、JavaScript で、適切なスレッドで呼び出されることを保保します `Function` 。</span><span class="sxs-lookup"><span data-stu-id="42fac-172">A Windows Runtime delegate is represented in JavaScript as a JavaScript `Function`, which is guaranteed to be called on the proper thread.</span></span>  <span data-ttu-id="42fac-173">引数が `Function` インブランクに入ると、同等の Windows ランタイム パラメーターの種類に変換されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-173">When the `Function` is invoked, the arguments are converted to the equivalent Windows Runtime parameter types.</span></span>  <span data-ttu-id="42fac-174">代理パラメーターよりも JavaScript 引数の数が少ない場合、代理呼 `in` び出しは失敗します。</span><span class="sxs-lookup"><span data-stu-id="42fac-174">If there are fewer JavaScript arguments than delegate `in` parameters, the delegate call fails.</span></span>  <span data-ttu-id="42fac-175">代理人にパラメーターが含めている JavaScript 引数が複数ある場合、 `in` 超 EvaScript 引数は無視されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-175">If there are more JavaScript arguments than there are `in` parameters in the delegate, the extra JavaScript arguments are ignored.</span></span>  <span data-ttu-id="42fac-176">代理人が非読み上げになると、パラメーターは `out` JavaScript の種類に変換され、返されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-176">After the delegate is invoked, the `out` parameters are converted to JavaScript types and returned.</span></span>  <span data-ttu-id="42fac-177">ネイティブ JavaScript 関数オブジェクトが Windows ランタイム代理人に変換されると、対応する型の Windows ランタイム代理人でそのオブジェクトがラップされます。</span><span class="sxs-lookup"><span data-stu-id="42fac-177">If a native JavaScript function object is converted to a Windows Runtime delegate, it is wrapped in a Windows Runtime delegate of the corresponding type.</span></span>  <span data-ttu-id="42fac-178">代理人が実行されると、パラメーターは `in` JavaScript の種類に変換されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-178">When the delegate is invoked, the `in` parameters are converted to JavaScript types.</span></span>  <span data-ttu-id="42fac-179">代理人が無効になりましたが、戻り値は代理人のパ `out` ラメーターに変換されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-179">After the delegate is invoked, the return value is converted to the `out` parameters of the delegate.</span></span>  |  
| <span data-ttu-id="42fac-180">インターフェイス</span><span class="sxs-lookup"><span data-stu-id="42fac-180">Interface</span></span> | `Object` | <span data-ttu-id="42fac-181">インターフェイスとインターフェイス グループは、オブジェクトとして JavaScript では直接表されません。</span><span class="sxs-lookup"><span data-stu-id="42fac-181">Interfaces and interface groups are not directly represented in JavaScript as objects.</span></span>  <span data-ttu-id="42fac-182">ただし、インターフェイスはパラメーターとして表され、Windows ランタイムメソッドの型を返します。</span><span class="sxs-lookup"><span data-stu-id="42fac-182">However, interfaces are represented as parameter and return types of Windows Runtime methods.</span></span>  <span data-ttu-id="42fac-183">Windows ランタイム インスタンスは次のように変換されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-183">A Windows Runtime interface instance is converted as follows:</span></span><br /> <br /> <span data-ttu-id="42fac-184">1. 有効なランタイム クラスがある場合は、そのクラスのインスタンスとしてオブジェクトを表します。</span><span class="sxs-lookup"><span data-stu-id="42fac-184">1.  If there is a valid runtime class, represent the object as an instance of that class.</span></span><br /> <span data-ttu-id="42fac-185">2. 有効なランタイム クラスがない場合は、インスタンスを実装し、必要なインターフェイスを実装するインターフェイスを実装するインターフェイスを実装する、名前なしランタイム クラスのインスタンスとしてオブジェクトを示します。</span><span class="sxs-lookup"><span data-stu-id="42fac-185">2.  If there is no valid runtime class, represent the object as an instance of an unnamed runtime class that implements exactly the interface that the instance is known to implement plus its required interfaces.</span></span><br /> <br /> <span data-ttu-id="42fac-186">JavaScript 値は、ランタイム クラスのインスタンスであるかインターフェイスのインスタンスであるかを判別するテストを行っています。</span><span class="sxs-lookup"><span data-stu-id="42fac-186">A JavaScript value is tested to determine whether it is an instance of a runtime class or an interface.</span></span>  <span data-ttu-id="42fac-187">その場合、元の Windows ランタイム値でインターフェイスが実装されている場合、そのオブジェクトは Windows ランタイムに合うようになります。</span><span class="sxs-lookup"><span data-stu-id="42fac-187">If it is, and the original Windows Runtime value implements the interface, the object is passed to the Windows Runtime.</span></span>  |  
| <span data-ttu-id="42fac-188">ランタイム クラス</span><span class="sxs-lookup"><span data-stu-id="42fac-188">Runtime classes</span></span> | `Object` | <span data-ttu-id="42fac-189">Windows ランタイムのオブジェクトは、1 つ以上のインターフェイスを実装するランタイム クラスのインスタンスです。</span><span class="sxs-lookup"><span data-stu-id="42fac-189">Objects in the Windows Runtime can be instances of runtime classes that implement one or more interfaces.</span></span>  <span data-ttu-id="42fac-190">Windows ランタイム オブジェクトは、オブジェクトとして JavaScript で表されます。</span><span class="sxs-lookup"><span data-stu-id="42fac-190">Windows Runtime objects are represented in JavaScript as objects.</span></span>  <span data-ttu-id="42fac-191">クラスで実装されたすべてのインターフェイスで定義されたメソッド、プロパティ、およびイベントのユニオンは、JavaScript オブジェクトのプロトタイプの名前付きプロパティを表します。</span><span class="sxs-lookup"><span data-stu-id="42fac-191">The union of methods, properties, and events defined on all the implemented interfaces of the class represents the named properties in the prototype of the JavaScript object.</span></span>  <span data-ttu-id="42fac-192">JavaScript のコンシューマーは、クラスの任意のメンバーに直接アクセスできます。</span><span class="sxs-lookup"><span data-stu-id="42fac-192">JavaScript consumers can access any member of the class directly.</span></span>  <span data-ttu-id="42fac-193">Windows ランタイム オブジェクトには、ランタイム クラスの実装されたすべてのインターフェイスからのすべてのメンバーを含むプロトタイプがあります。</span><span class="sxs-lookup"><span data-stu-id="42fac-193">Windows Runtime objects have a prototype that contains all the members from all the implemented interfaces of the runtime class.</span></span>  |  
  
## <span data-ttu-id="42fac-194">関連項目</span><span class="sxs-lookup"><span data-stu-id="42fac-194">See also</span></span>  

[<span data-ttu-id="42fac-195">JavaScript での Windows ランタイムの使用</span><span class="sxs-lookup"><span data-stu-id="42fac-195">Using the Windows Runtime in JavaScript</span></span>][WindowsRuntimeJavascript]  

<!-- links -->  

[WindowsRuntimeJavascript]: ./using-the-windows-runtime-in-javascript.md "JavaScript で Windows ランタイムを使用する |Microsoft ドキュメント"  
